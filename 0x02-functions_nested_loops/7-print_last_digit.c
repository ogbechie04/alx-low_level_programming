#include "main.h"

/**
 * print_last_digit - prints the last digit of a number
 *
 * @i: integer whose last digit would be printer
 *
 * Return: returns last digit
 */

int print_last_digit(int i)
{
	return (i % 10);
}
