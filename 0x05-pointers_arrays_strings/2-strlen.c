#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string
 *
 * @s: string
 *
 * Return: result
 */

int _strlen(char *s)
{
	int lenght;

	lenght = strlen(s);

	return (lenght);
}
